<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>
<p>
  <strong>Image:</strong>
  <% if @category.image.attached? %>
  <%= image_tag @category.image.variant(resize: "300x300") %>
  <%end%>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
